---
name: aitp
description: Explicit short entrypoint for the installed AITP Research Memory plugin. Use only when the user invokes $aitp to enter, initialize, read, record, or write Notes in a theoretical-physics research workspace.
---

# AITP

Read `../using-aitp/SKILL.md` completely, resolve all relative paths from that Skill's directory, and follow it for the user's request.

If the user supplies no operation, run the bundled `aitp enter` workflow for the current workspace and return its grounded recorded state.
