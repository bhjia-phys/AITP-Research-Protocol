interface:
  display_name: "AITP"
  short_description: "Explicit entrypoint for AITP research memory"
  default_prompt: "Use $aitp to enter this research workspace and manage its durable memory."
policy:
  allow_implicit_invocation: false
