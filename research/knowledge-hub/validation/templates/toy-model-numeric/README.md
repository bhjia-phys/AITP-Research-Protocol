# Toy-Model Numeric Templates

This directory stores public starter templates for small, reproducible
toy-model calculations.

Current starter:

- `tfim-gap.config.template.json`

The intent is:

- keep the exact model definition explicit,
- keep parameter choices small and auditable,
- keep config files separate from result summaries,
- keep the final note human-readable enough to register into `L0`.
