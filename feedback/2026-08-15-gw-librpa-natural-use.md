# GW/LibRPA 自然使用反馈（2026-08-15）

- **Topic / 日期**：`/home/bhjia/physics/GW_librpa`，2026-08-15；同一长周期课题的继续自然使用，非 gate/spec/对照实验。
- **多 workstream / scoped check 有帮助**：全局 `aitp check --json` 报 327 entries/6 notes、217 errors/2 warnings；`check --workstream magnetic-symmetry` 缩到 39 entries/4 notes、仅 2 errors，`--workstream qsgw-semiconductor` 为 14 entries/16 errors，`--workstream crpa` 为 0/0/clean——单线会话可只看本线债务，但 327 条中仅 54 条带 `workstreams`（39+14+1 条双标签 validation/provenance，0 条 crpa），空 scope 的 clean 不代表该线健康。
- **prepare/save 仍需手工编辑 YAML**：延续 08-14 观察，`record prepare` 模板仍无必填键 `at` 的示例，按 `pin:` 填写后 `record save` 以 `error[invalid_ref]: refs[0].at is required` 失败；producer→RPA→G0W0→finalizer 与 Si preflight r3→r4 的密集结论链仍是 prepare→开 draft→手工填→save 逐条全流程。
- **远端 pointer/hash 需人工维护**：`check` 对 entry-0e956f03... 报 `qsgw/semiconductor-validation/2026-08-14-dongfang-recovery/preflight/2026-08-15-si-r4/MANIFEST.sha256`（expected 8ac5e14f, actual f68854e0）与 `binary-pair.json` hash_mismatch，entry-0ff2f255...（submission-20260810-0503-abacus-r9-job-2539642.md）、entry-0316ff59...（PROJECT_MEMORY.md）同样 drift——本地 pointer 文件随 r3→r4 修订更新后，Entry 内 pin 未同步，全部以 error 呈现。
- **旧 Note hash drift 会制造 scoped finding**：magnetic-symmetry 的 2 条 errors 全部来自 Note：note-67e28528... 对 `symmetry/jhep-note/main.pdf`（expected 0dcb26db, actual c29040ee）与 `main.tex`（expected 6da1c260, actual 41284b2e）的 pin 落后；note-25607ad2... 与 note-4d7108ba... 对 `group-meeting-soc-magnetic-gw-summary.md` 的 pin（f31a7b4d/692d86d8 vs actual 5c6ade86）同样过期，Note 级旧 pin 直接污染本线 scoped 报告。
- **closeout-first handoff 对新 failure 可能滞后**：`enter` 的 `next_action` 仍来自 2026-08-14T13:40:21Z 的 closeout entry-ad4be166...（"Fix and re-verify the finalizer analyzer contract"），但其后已有 8 条更新记录（`active_newer: 8`），最新 result entry-681027c5...（18:07Z）已判 Fe r19 finalizer r2 为 COMPLETE + scientific PASS，且 `unresolved_failures` 升至 33——handoff 不自动跟随新 failure/result。
- **本次 Fisherd 迁移暴露的运维边界**：AITP 能保存来源、结果和失败链，但不会观察后台 `rsync`/SSH、报告传输进度、发现 partial-dir/权限错误，也不会自动给迁移建立完整性状态；这些仍需 `TaskList`/日志、清单和 checksum 手工闭环。
- **跨工具操作缺少统一 run/result 适配**：迁移任务、Slurm 作业和远端 pointer 的生命周期不进入 AITP 的结构化状态，研究结论与运维证据只能靠手写 Entry 和本地 manifest 连接；若不补写 closeout，下一次 `enter` 很容易回到旧 handoff。
