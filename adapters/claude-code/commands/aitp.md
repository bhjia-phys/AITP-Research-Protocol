# /aitp

User request: $ARGUMENTS

1. If the topic exists, run `aitp resume --topic-slug <topic_slug> --human-request "$ARGUMENTS"`.
2. If the topic is new, run `aitp bootstrap --topic "<topic>" --statement "$ARGUMENTS"`.
3. Read `runtime_protocol.generated.md`, `agent_brief.md`, `operator_console.md`, and `conformance_report.md`.
4. Continue only after the runtime artifacts exist.
