# Roadmap: v1.66 PyPI Publishable Package

## Result

`v1.66` is complete.

## Phases

- [x] **Phase 131: Package Identity And Distribution Asset Contract**
- [x] **Phase 132: PyPI Install Surface And Migration**
- [x] **Phase 133: Closure And Clean-Install Audit**

## Outcome

Production outcome:

- one publishable `aitp-kernel` package contract that keeps the `aitp` CLI name
- one packaged runtime-bundle surface that no longer depends on a repo-local
  editable install for newcomers
- one clean-install smoke proof that verifies the installed wheel on an
  isolated virtualenv

## Next Step

Select the next milestone from backlog without reopening `999.48` through
`999.51` as substitute work.
