# Roadmap: v1.87 Hypothesis Route Transition Followthrough Surface

## Result

`v1.87` is complete.

## Overview

`v1.87` closed the next post-`v1.86` research-control gap around
hypothesis-aware route transition follow-through. It derived one bounded
`route_transition_followthrough` summary from the current route transition
clearance plus existing transition refs, surfaced the bounded follow-through
state through runtime status and replay, and proved the bounded coexistence
path with a dedicated isolated acceptance lane.

**Phases:** 161
**Total Plans:** 1
**Total Tasks:** 2
**Shipped:** 2026-04-12

## Phase Details

### Phase 161: Hypothesis Route Transition Followthrough Surface

**Goal:** Add one durable route transition-followthrough summary for the
current post-clearance next-step state and expose it through
runtime/read-path/replay flows without widening into fresh runtime mutation.

**Plans:** 1 plan

Plans:

- [x] `161-01`: add route transition-followthrough summary to runtime/replay
  surfaces and isolated acceptance

**Outcome:**

- landed `route_transition_followthrough` payload construction and rendering on
  the runtime/read-path layer
- surfaced `none`, `held_by_clearance`, and `ready` state through runtime
  status, the runtime protocol note, and replay
- aligned schema, docs, and isolated acceptance coverage for the bounded route
  transition-followthrough path

## Milestone Summary

**Key Accomplishments:**

- route transition clearance and existing transition refs now join into one
  explicit bounded follow-through surface
- runtime status and replay now keep held-versus-ready post-clearance state
  visible without replacing transition clearance or helper mechanisms
- one isolated acceptance lane now proves bounded transition-followthrough
  visibility stays declarative and does not widen into fresh runtime mutation

**Issues Resolved:**

- closed the gap where operators could see clearance but still had to infer
  what bounded transition work should resume next
- closed the gap where post-clearance continuation still had no explicit
  transition-followthrough surface

**Issues Deferred:**

- explicit route-transition resumption after follow-through becomes ready
- automatic route commit and follow-up runtime mutation
- automatic branch spawning and multi-branch scheduling

**Technical Debt / Honest Gaps:**

- the first slice makes post-clearance follow-through visible, but it does not
  yet surface one explicit resumption lane for whether that follow-through has
  been durably re-entered on the bounded route

---

For current planning state after `v1.87`, continue from the next fresh
milestone definition.
