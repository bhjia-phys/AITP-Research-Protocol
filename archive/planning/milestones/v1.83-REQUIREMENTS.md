# Requirements: v1.83 Hypothesis Route Transition Discrepancy Surface

## Milestone Goal

Close the next post-`v1.82` research-control gap so AITP can turn inconsistent
route-transition state into one operator-visible discrepancy surface, instead of
forcing operators to compare resolution, receipt, and active-route artifacts by
hand.

## Completed Requirements

### Route Transition-Discrepancy Surface

- [x] `REQ-HDISC-01`: runtime surfaces can materialize a bounded route
  transition-discrepancy summary from route transition resolution plus upstream
  route artifacts.
- [x] `REQ-HDISC-02`: replay/read paths expose whether discrepancy is present
  directly instead of leaving that discrepancy implicit.

### Coexistence Rule

- [x] `REQ-HDISC-03`: the route transition-discrepancy surface coexists cleanly
  with the current route_transition_resolution surface and helper mechanisms
  instead of auto-triggering fresh runtime mutation.

### Verification

- [x] `REQ-VERIFY-01`: the milestone closes with one bounded isolated
  route transition-discrepancy acceptance path plus targeted regression coverage
  for the new discrepancy surface.

## Requirement Outcomes

- `REQ-HDISC-01`: validated through `route_transition_discrepancy` payload
  materialization on the active research contract plus runtime protocol
  rendering.
- `REQ-HDISC-02`: validated through replay visibility for present versus none
  transition discrepancy.
- `REQ-HDISC-03`: validated through the bounded acceptance lane that keeps
  route transition discrepancy, transition resolution, and helper mechanisms
  visible without fresh runtime mutation.
- `REQ-VERIFY-01`: validated through the dedicated isolated acceptance lane and
  targeted replay/schema/runtime regression slices.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| REQ-HDISC-01 | Phase 157 | Complete |
| REQ-HDISC-02 | Phase 157 | Complete |
| REQ-HDISC-03 | Phase 157 | Complete |
| REQ-VERIFY-01 | Phase 157 | Complete |
