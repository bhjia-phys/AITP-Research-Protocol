# Requirements: v1.97 First Positive L0 To L2 Closure

## Milestone Goal

Prove that one positive real-topic lane can travel from the public AITP front
door through bounded source and validation work and land one authoritative unit
in canonical `L2`, while making the L2 compiler and consultation surfaces
trustworthy enough to serve as the next multi-mode baseline.

## Archived Requirements

### Positive Lane Closure

- [x] `REQ-L2POS-01`: one fresh `formal_derivation` topic enters through the
  public front door, completes bounded source intake and validation work, and
  promotes one authoritative unit into canonical `L2` with a durable backend
  receipt.
  Final outcome: satisfied by Phase `171`.

- [x] `REQ-L2POS-02`: the promoted positive unit exposes the same authority
  state across the promotion gate, canonical writeback location, compiled L2
  knowledge report, workspace memory map or graph report, and `consult-l2`.
  Final outcome: satisfied across Phases `171` and `171.1`.

### L2 Surface Hardening

- [x] `REQ-L2POS-03`: L2 compiler and read surfaces preserve honest
  coexistence between the new authoritative positive unit and the existing
  `negative_result -> contradiction_watch` row without losing provenance,
  authority labels, or retrieval visibility.
  Final outcome: satisfied by Phase `171.1`.

- [x] `REQ-L2POS-04`: one replayable acceptance lane proves the full positive
  route mechanically from public bootstrap through authoritative L2 writeback.
  Final outcome: satisfied by Phase `171.2`.

### Carry-Over Routing

- [x] `REQ-L2POS-05`: the milestone closes with explicit deferred blockers and
  next actions for `toy_model` and `first_principles` positive-L2 closure, so
  widening back to multiple paradigms happens on a firmer L2 baseline.
  Final outcome: satisfied by Phase `171.2`.

## Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| REQ-L2POS-01 | Phase 171 | Satisfied |
| REQ-L2POS-02 | Phase 171 / 171.1 | Satisfied |
| REQ-L2POS-03 | Phase 171.1 | Satisfied |
| REQ-L2POS-04 | Phase 171.2 | Satisfied |
| REQ-L2POS-05 | Phase 171.2 | Satisfied |

## Outcome Notes

- Phase `171` landed the first fresh positive authoritative theorem in
  repo-local canonical `L2`
- Phase `171.1` proved that authoritative positive rows and staged
  contradiction rows can coexist honestly on compiled and consultation
  surfaces
- Phase `171.2` turned that proof into a replayable baseline and made the next
  widening actions explicit for the `HS model` and `LibRPA QSGW` lanes

_Archived after milestone closure on 2026-04-14._
