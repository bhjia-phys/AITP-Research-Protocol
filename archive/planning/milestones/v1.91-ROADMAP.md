# Roadmap: v1.91 Real Topic L0 To L2 End-To-End Validation

## Result

`v1.91` closed successfully.

## Phases

- [x] **Phase 165: Real Topic L0 To L2 End-To-End Validation**
- [x] **Phase 165.1: Proof Engineering Knowledge Layer — Schema + Bootstrap**
- [x] **Phase 165.2: Mode Envelope Runtime Enforcement + Literature Intake Fast Path**
- [x] **Phase 165.3: HCI Foundation — Human-Readable Surfaces + Onboarding**
- [x] **Phase 165.4: Agent Governance — Mechanical Verification + Schema Isolation**
- [x] **Phase 165.5: L0/L1 Integration — DeepXiv Progressive Reading + Graphify Concept Graph**
- [x] **Phase 165.6: Milestone Closure Gap — Public Front Door Real-Topic Proof**

## Target Outcome

- one real-topic run from an initial idea through the current public AITP
  entry surfaces
- one durable postmortem naming where the protocol helped, where it created
  friction, and what bounded outcome was actually reached
- one explicit issue ledger that routes discovered problems into GSD-visible
  follow-up work instead of leaving them implicit
- mode envelope enforcement that actually controls runtime behavior per mode
- a literature-intake fast path that lets papers deposit knowledge into L2
  staging without the full formal-theory audit pipeline
- human-readable status/next rendering as the default CLI output
- mechanical-first verification before expensive LLM evaluation

## Completion Summary

`v1.91` closed by combining one honest real-topic issue-discovery pass with the
follow-up runtime, governance, and `L0 -> L1` integration work that the run
exposed, then finishing with a fresh public-front-door closure proof.

### Phase 165: Real Topic L0 To L2 End-To-End Validation

**Goal:** Run one real topic through the current AITP user-facing flow, capture
the honest bounded outcome, and route every discovered problem into explicit
post-E2E engineering work.
**Depends on:** `v1.90`
**Plans:** 1 plan

Plans:

- [x] `165-01` Execute one real-topic E2E route and capture all findings into
  GSD-visible artifacts

### Phase 165.1: Proof Engineering Knowledge Layer — Schema + Bootstrap

**Goal:** Define the `proof_fragment` canonical schema, seed
`strategy_memory.jsonl` with Jones-topic discoveries, and create the first
canonical proof-fragment instance.
**Depends on:** `165`
**Plans:** 1 shipped slice

Plans:

- [x] `165.1-01` Define proof_fragment schema + seed strategy_memory + create
  first instance

### Phase 165.2: Mode Envelope Runtime Enforcement + Literature Intake Fast Path

**Goal:** Make the four AITP modes materially control runtime behavior and add
the lightweight literature-intake fast path into L2 staging.
**Depends on:** `165`
**Plans:** 1 plan file, 2 shipped slices

Plans:

- [x] `165.2-01` Mode-aware runtime bundle + escalation sensitivity
- [x] `165.2-02` Literature-intake fast path + L2 staging bridge

### Phase 165.3: HCI Foundation — Human-Readable Surfaces + Onboarding

**Goal:** Replace machine-first operator surfaces with bounded human-first entry
points, help surfaces, steering, and failure output.
**Depends on:** `165`
**Plans:** delivered as shipped closure slices

Delivered slices:

- [x] `999.60` compact human-readable `status` / `next`
- [x] `999.61` `aitp hello` onboarding wrapper
- [x] `999.62` checkpoint-surface jargon cleanup
- [x] `999.63` progressive-disclosure help surface
- [x] `999.64` natural-language topic steering
- [x] `999.70` human-friendly subprocess failure output

### Phase 165.4: Agent Governance — Mechanical Verification + Schema Isolation

**Goal:** Replace prompt-level governance with mechanical enforcement,
observability, and durable artifact contracts.
**Depends on:** `165.3`
**Plans:** delivered as shipped closure slices

Delivered slices:

- [x] `999.73` mechanical completion preflight
- [x] `999.74` context injection with dedup
- [x] `999.75` schema-level agent isolation
- [x] `999.76` theory-operation metrics and offline analyzer
- [x] `999.77` derivation loop detection with intervention
- [x] `999.78` manifest-as-truth-source

### Phase 165.5: L0/L1 Integration — DeepXiv Progressive Reading + Graphify Concept Graph

**Goal:** Integrate DeepXiv-style progressive reading and Graphify-style concept
graphs into AITP's `L0 -> L1` path and make the graph useful for `L1 -> L2`
staging plus operator navigation.
**Depends on:** `165.2`
**Plans:** 3 plans

Plans:

- [x] `165.5-01` Post-registration enrichment + concept graph construction + MIT attribution
- [x] `165.5-02` Progressive reading + graph-based intake extension + graph analysis tools
- [x] `165.5-03` Obsidian export path + graph navigation + external brain bridge

### Phase 165.6: Milestone Closure Gap — Public Front Door Real-Topic Proof

**Goal:** Close the remaining blocker by proving one real topic can enter
through the public AITP front door with a fresh slug and leave the same durable
evidence trail required by the milestone.
**Depends on:** `165`, `165.2`, `165.3`, `165.4`, `165.5`
**Plans:** 1 plan

Plans:

- [x] `165.6-01` Run one public-front-door real-topic closure lane and refresh
  the milestone evidence + planning truth surfaces

## Milestone Summary

**Key accomplishments:**

- one honest real-topic Jones route now has durable postmortem and issue-ledger
  evidence instead of chat-only conclusions
- `proof_fragment` is no longer a reserved ontology family without a schema or
  first instance
- the runtime modes, human-facing CLI surfaces, and mechanical governance
  surfaces now materially reflect the real-topic friction discovered by the
  milestone
- `L0 -> L1` now includes DeepXiv-style enrichment, concept graphs,
  progressive reading, graph analysis, and Obsidian-compatible export
- the public AITP front door now has a fresh real-topic proof with an honest
  bounded return-to-`L0` outcome

**Issues deferred:**

- `999.52` broader proof-engineering memory distillation beyond the first Jones
  seed
- `999.86` more concrete `L0` source-acquisition handoff after public bootstrap

_For current project status after milestone closure, start the next milestone
with `$gsd-new-milestone`._
