# Requirements: v1.70 L1 Source Assumptions And Reading Depth Closure

## Milestone Goal

Close the remaining `999.27` intake gap so AITP keeps source-backed
assumptions, reading-depth limits, and shallow/conflicting evidence visible
through the real `L1` operator surfaces rather than only as internal helper
rows.

## Completed Requirements

### L1 Assumption Surface

- [x] `REQ-L1INTAKE-01`: the repository exposes one explicit assumption-and-
  reading-depth `L1` surface through production status/runtime artifacts,
  centered on the existing `l1_source_intake` path.
- [x] `REQ-L1INTAKE-02`: the operator-facing `research_question.contract`,
  runtime read path, and `L1` vault all keep source-backed assumptions plus
  recorded reading depth visible without creating a second competing intake
  artifact.

### Honesty And Conflict Handling

- [x] `REQ-L1INTAKE-03`: when sources disagree, remain skim-only, or stay
  otherwise weakly grounded, `L1` surfaces keep those limitations explicit
  instead of flattening them into one stronger claim.

### Verification

- [x] `REQ-VERIFY-01`: the milestone closes with one bounded isolated
  assumption/depth acceptance path plus targeted regression coverage for the
  new operator surface.

## Requirement Outcomes

- `REQ-L1INTAKE-01`: validated through the strengthened `l1_source_intake`
  render path on the research contract, runtime protocol note, and dashboard.
- `REQ-L1INTAKE-02`: validated through the same assumption/depth surface across
  `research_question.contract.md`, `topic_dashboard.md`,
  `runtime_protocol.generated.md`, and `intake/.../vault/wiki/source-intake.md`.
- `REQ-L1INTAKE-03`: validated through explicit reading-depth limits and
  contradiction candidates instead of flattened summary prose.
- `REQ-VERIFY-01`: validated through the dedicated isolated acceptance lane and
  targeted regression slices.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| REQ-L1INTAKE-01 | Phase 144 | Complete |
| REQ-L1INTAKE-02 | Phase 144 | Complete |
| REQ-L1INTAKE-03 | Phase 144 | Complete |
| REQ-VERIFY-01 | Phase 144 | Complete |
