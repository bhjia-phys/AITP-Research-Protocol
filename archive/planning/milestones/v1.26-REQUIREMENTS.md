# Requirements: v1.26 Interaction-Surface And Chat-Routing Boundary Extraction And Closure

## Shipped requirements

- **POL-18**: The operator-facing interaction-state synthesis centered on
  `build_interaction_state`, `build_operator_console`, and
  `build_agent_brief` has left `orchestrate_topic.py` for a dedicated support
  boundary.
- **MTN-22**: The chat-routing and session-start cluster centered on
  `route_codex_chat_request` and `start_chat_session` has left
  `aitp_service.py` for a dedicated support boundary.
- **DOC-26**: Docs, targeted runtime-script/service regressions, and
  maintainability budgets now lock both the extracted interaction-surface
  boundary and the extracted chat-routing boundary.
