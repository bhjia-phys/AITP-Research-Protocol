# Roadmap: v1.96 Real Topic Promotion E2E Proof

## Result

`v1.96` closed successfully as a mode-bootstrap and negative-result proof
milestone. The originally planned positive `L0 -> L2` promotion proof was
honestly deferred.

## Phases

- [x] **Phase 170: Three-Mode Positive Promotion Proof Lane**
- [x] **Phase 170.1: Negative-Result Promotion Proof Lane**
- [x] **Phase 170.2: E2E Evidence And Regression Closure**

## Delivered Outcome

- three fresh public-front-door topic runs proved that `formal_derivation`,
  `toy_model`, and `first_principles` all bootstrap honestly and pass the same
  `27/27` conformance surface
- one honest failed route landed as canonical `negative_result` staging and
  compiled as `contradiction_watch`
- all four lanes now have durable receipts, replay instructions, and an honest
  cross-lane postmortem

## Completion Summary

`v1.96` closed by first broadening the original single-lane positive proof into
three mode-specific front-door lanes, then proving the repaired negative-result
path on a real physics failure, and finally turning all four lanes into
replayable evidence with explicit non-proof statements about what still did not
reach `L2`.

### Phase 170: Three-Mode Positive Promotion Proof Lane

**Goal:** Exercise the public AITP front door across the three target research
modes and record honest positive-lane receipts.
**Depends on:** `v1.95`
**Plans:** 1 plan

Plans:

- [x] `170-01` Run three research-mode positive proof lanes and record durable
  bootstrap receipts

**Actual result:** three fresh topics reached `L3` cleanly with mode-correct
bootstrap and conformance, but none completed positive promotion into
authoritative canonical `L2`.

### Phase 170.1: Negative-Result Promotion Proof Lane

**Goal:** Prove that one bounded failed route can land durably as
`negative_result`.
**Depends on:** Phase `170`
**Plans:** 1 plan

Plans:

- [x] `170.1-01` Run one honest negative-result promotion proof into canonical
  staging and compile it onto the L2 read path

**Actual result:** the HS-model OTOC Lyapunov-exponent failure was recorded as
`failure_kind: regime_mismatch`, staged as `negative_result`, and compiled as
`contradiction_watch`.

### Phase 170.2: E2E Evidence And Regression Closure

**Goal:** Turn all proof lanes into replayable evidence with durable receipts
and explicit postmortem context.
**Depends on:** Phase `170` and Phase `170.1`
**Plans:** 1 plan

Plans:

- [x] `170.2-01` Close all proof lanes with durable receipts, replay evidence,
  and postmortem surfaces

## Milestone Summary

**Key accomplishments:**

- the public front door now has real-topic bootstrap evidence across
  `formal_derivation`, `toy_model`, and `first_principles`
- the repaired `negative_result` path from `v1.95` is now proven on a real,
  physically honest failure case
- the same `27/27` conformance surface held across all three positive-mode
  topics and the negative-result topic
- all four proof lanes now have durable receipts and a single replay guide

**Issues deferred:**

- one full positive `L0 -> L2` route still needs to land an authoritative unit
- positive promotion receipt parity on runtime/read-path surfaces remains
  deferred until that authoritative positive promotion exists
- `first_principles -> code_method` mapping remains unproven
- toy-model convergence or benchmark acceptance remains unproven

_Archived after milestone closure on 2026-04-14._
