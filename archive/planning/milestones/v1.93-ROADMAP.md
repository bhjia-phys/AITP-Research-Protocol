# Roadmap: v1.93 L1 Contradiction Adjudication Surface

## Result

`v1.93` closed successfully.

## Phases

- [x] **Phase 167: L1 Contradiction Intake Rows And Comparison Basis**
- [x] **Phase 167.1: Contradiction Runtime Surface And Proof Lane**

## Target Outcome

- `L1` intake makes contradictory or mutually incompatible source claims
  explicit instead of leaving them spread across unrelated assumption rows
- each contradiction row carries enough bounded evidence context for a human to
  understand why the conflict was surfaced
- runtime-facing read paths expose the same contradiction surface across
  `status`, `runtime_protocol`, and operator-facing `L1` views
- one bounded contradiction-aware multi-source intake proof closes the milestone

## Completion Summary

`v1.93` closed by first upgrading the existing `L1` contradiction row contract
into richer, source-backed contradiction rows with explicit comparison basis,
then carrying that same contradiction surface across runtime/read-path outputs
and closing with a dedicated contradiction-aware proof lane.

### Phase 167: L1 Contradiction Intake Rows And Comparison Basis

**Goal:** Extend `L1` intake so incompatible claims, assumptions, or regime
statements across sources become first-class contradiction rows rather than
remaining implicit in separate source summaries.
**Depends on:** `v1.92`
**Plans:** 1 plan

Plans:

- [x] `167-01` Add contradiction rows and bounded comparison context to `L1`
  intake artifacts

### Phase 167.1: Contradiction Runtime Surface And Proof Lane

**Goal:** Expose the new contradiction surface on runtime read paths and leave
one bounded contradiction-aware intake proof.
**Depends on:** Phase `167`
**Plans:** 1 plan

Plans:

- [x] `167.1-01` Surface contradiction rows across runtime read paths and prove
  one bounded multi-source contradiction lane

## Milestone Summary

**Key accomplishments:**

- `L1` contradiction rows are now richer, source-backed, and pairwise
- contradiction rows now carry bounded comparison basis, side-specific
  summaries, and evidence excerpts
- runtime/read-path contradiction parity now survives across `status`,
  `runtime_protocol`, dashboard, and `L1` vault source-intake
- the milestone closes with a dedicated contradiction-aware proof lane

**Issues deferred:**

- full scientific adjudication of which source is correct
- queue-level contradiction rerouting or checkpoint automation
- broader analytical validation beyond contradiction visibility

_For current project status after milestone closure, start the next milestone
with `$gsd-new-milestone`._
