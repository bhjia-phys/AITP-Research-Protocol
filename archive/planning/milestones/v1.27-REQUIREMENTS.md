# Requirements: v1.27 Capability-Audit And Runtime-Sync Boundary Extraction And Closure

## Shipped requirements

- **MTN-23**: The capability-audit cluster centered on
  `AITPService.capability_audit()` has left `aitp_service.py` for a dedicated
  support boundary.
- **POL-19**: The runtime-sync explainability/resume-render cluster centered on
  `infer_resume_state`, `derive_status_explainability`, and
  `build_resume_markdown` has left `sync_topic_state.py` for a dedicated
  support boundary.
- **DOC-27**: Docs, targeted runtime-script/service regressions, and
  maintainability budgets now lock both the extracted capability-audit
  boundary and the extracted runtime-sync boundary.
