# Requirements: v1.18 Declarative Append Policy Closure And Promotion-Gate Boundary Seed

## Active requirements

- **POL-19**: Declared `append_runtime_actions=false` suppresses all
  helper-produced `runtime_appended` system actions.
- **POL-20**: Active operator checkpoints suppress those helper-produced
  runtime-appended system actions too, while capability-gap skill append stays
  separately governed.
- **MTN-15**: The shared promotion-gate lifecycle has left
  `aitp_service.py` for a dedicated support boundary.
- **DOC-18**: Docs, targeted regressions, and maintainability budgets should
  lock the milestone.
