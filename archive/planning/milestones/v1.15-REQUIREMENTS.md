# Requirements: v1.15 Queue Materialization And Auto-Action Boundary Extraction

## Shipped requirements

- **POL-13**: Queue materialization in `orchestrate_topic.py` now consumes the
  explicit runtime contract for the first bounded preference cases.
- **MTN-13**: The auto-action execution boundary has moved out of
  `aitp_service.py` into a dedicated support module.
- **DOC-15**: Regression coverage and maintainability budgets now lock both
  the queue-materialization slice and the extracted auto-action boundary.
