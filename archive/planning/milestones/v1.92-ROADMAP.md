# Roadmap: v1.92 Public Front Door Source Handoff

## Result

`v1.92` closed successfully.

## Phases

- [x] **Phase 166: Public Front Door L0 Source Handoff**
- [x] **Phase 166.1: Contentful Source Registration Default**

## Target Outcome

- after a fresh public `bootstrap` returns a topic to `L0 source expansion`,
  the operator can see one concrete next source-acquisition path instead of only
  generic prose
- `status`, `runtime_protocol`, and `replay-topic` all expose the same concrete
  `L0` handoff surface
- arXiv registration downloads source content by default, with
  `--metadata-only` as the explicit lightweight opt-out
- one bounded acceptance lane proves
  `bootstrap -> concrete source handoff -> source registration` on a fresh
  topic

## Completion Summary

`v1.92` closed by first making the honest public-front-door `return_to_L0`
surface concrete across runtime truth surfaces, then turning the default arXiv
registration path into a contentful-by-default continuation and proving the
full `bootstrap -> handoff -> registration` lane on a fresh topic.

### Phase 166: Public Front Door L0 Source Handoff

**Goal:** Turn the honest post-bootstrap `return_to_L0` outcome into one
concrete operator-facing source-acquisition handoff instead of leaving the next
step as generic prose.
**Depends on:** `v1.91` closure
**Plans:** 1 plan

Plans:

- [x] `166-01` Add concrete source-acquisition handoff surfaces after public
  bootstrap

### Phase 166.1: Contentful Source Registration Default

**Goal:** Make arXiv registration contentful-by-default so the
source-acquisition handoff does not immediately drop the user into
metadata-only registration.
**Depends on:** Phase `166`
**Plans:** 1 plan

Plans:

- [x] `166.1-01` Make arXiv registration contentful-by-default and prove the
  fresh-topic handoff path

## Milestone Summary

**Key accomplishments:**

- a fresh public bootstrap now exposes one concrete `L0` recovery lane instead
  of generic prose
- dashboard, runtime protocol, and replay now share the same concrete
  `l0_source_handoff` truth surface
- arXiv registration and discovery-driven registration now attempt content
  acquisition by default while preserving `--metadata-only` for lightweight
  use
- the milestone leaves one durable fresh-topic proof of
  `bootstrap -> handoff -> registration` on the real repository kernel root

**Issues deferred:**

- broader `L0` provider expansion beyond the shipped arXiv-first entry surfaces
- automatic source acquisition or queue-level source execution
- unrelated post-`v1.91` HCI and recovery backlog items outside this bounded
  milestone

_For current project status after milestone closure, start the next milestone
with `$gsd-new-milestone`._
