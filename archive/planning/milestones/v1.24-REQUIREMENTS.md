# Requirements: v1.24 Source Distillation Boundary Extraction And Closure

## Shipped requirements

- **MTN-21**: The shared source-distillation lifecycle centered on
  `_distill_from_sources` has left `aitp_service.py` for a dedicated support
  boundary.
- **DOC-24**: Docs, targeted source-backed topic-start regressions, and
  maintainability budgets now lock the extracted source-distillation boundary.
