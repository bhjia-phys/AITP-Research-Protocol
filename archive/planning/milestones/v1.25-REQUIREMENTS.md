# Requirements: v1.25 Contract-Aware Checkpoint/Append Closure And Loop-Execution Boundary Extraction

## Shipped requirements

- **POL-17**: `orchestrate_topic.py` now uses explicit runtime and checkpoint
  artifacts for broader checkpoint/append orchestration logic.
- **MTN-12**: The loop/execution cluster centered on `run_topic_loop` has left
  `aitp_service.py` for a dedicated support boundary.
- **DOC-25**: Docs, targeted runtime-script/service regressions, and
  maintainability budgets now lock both the broader contract-aware
  orchestrator slice and the extracted loop boundary.
