# Requirements: v1.17 Checkpoint-Aware Queue Shaping And Auto-Promotion Extraction

## Shipped requirements

- **POL-18**: Queue shaping now respects `requires_human_checkpoint` in the
  explicit runtime contract for narrow runtime-appended suppression cases.
- **MTN-14**: `auto_promote_candidate` has moved out of `aitp_service.py`
  into a dedicated support module.
- **DOC-17**: Regression coverage and maintainability budgets now lock both
  the checkpoint-aware shaping slice and the extracted auto-promotion
  boundary.
