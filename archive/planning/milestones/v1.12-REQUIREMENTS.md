# Requirements: v1.12 Runtime Mode And Transition Contract Seed

## Shipped requirements

- **POL-04**: The runtime bundle now exposes explicit `runtime_mode` and
  `active_submode` instead of leaving mode selection implicit.
- **POL-05**: The runtime bundle now exposes explicit `mode_envelope` and
  `transition_posture` structures grounded in the doctrine docs.
- **DOC-12**: Schema, runtime docs, and regression tests now lock the new
  mode/transition contract.
