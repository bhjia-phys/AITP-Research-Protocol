# Phase 165.5-03 CLI Brain Bridge Summary

Status: implemented in working tree

## Goal

Close the next `165.5-03 / 999.84` slice by exposing the explicit
theoretical-physics-brain graph mirror as an operator-facing CLI command,
instead of leaving it reachable only through support code or direct service
calls.

## What Landed

- new CLI handler:
  - `knowledge_hub/cli_l1_graph_handler.py`
- new operator-facing command:
  - `aitp sync-l1-graph-export-to-theoretical-physics-brain --topic-slug <slug>`
- supported flags:
  - `--topic-slug`
  - `--target-root`
  - `--updated-by`
  - `--json`
- `aitp_cli.py` now registers and dispatches the command through the same
  structured handler pattern used by other bounded command families
- CLI e2e coverage now proves that the command can:
  - resolve the configured brain backend root
  - mirror the repo-local export
  - return the sync receipt payload as JSON

## Verification

- `python -m pytest research/knowledge-hub/tests/test_obsidian_graph_bridge_support.py -q`
- `python -m pytest research/knowledge-hub/tests/test_aitp_cli.py -q -k "sync_l1_graph_export_to_theoretical_physics_brain"`
- `python -m pytest research/knowledge-hub/tests/test_aitp_cli_e2e.py -q -k "sync_l1_graph_export_to_theoretical_physics_brain_cli_json_path"`
- `python -m pytest research/knowledge-hub/tests/test_runtime_scripts.py -q -k "obsidian_export or multicommunity or brain_bridge"`
- `python -m pytest research/knowledge-hub/tests/test_aitp_service.py -q -k "sync_l1_graph_export_to_theoretical_physics_brain"`

## Outcome

`165.5-03` now has an operator-facing closure slice:

- the external theoretical-physics brain bridge is no longer hidden behind
  internal helpers
- operators now have one explicit command to mirror the current topic graph
  export and inspect the resulting receipt
- the bridge remains explicit, side-effectful, and auditable rather than being
  silently folded into ordinary `status` materialization

## Remaining Open Work Inside 165.5-03 / 999.84

- decide whether `165.5-03` is now complete enough to close and move to the
  next phase, or whether a markdown receipt note is still worth adding
