# Phase 89 Summary

Status: implemented on `main`

## Goal

Publish the consolidated install index with the shared Python floor and clear
links to runtime-specific install, migration, and uninstall docs.

## What Landed

- new shared install index: `docs/INSTALL.md`
- explicit `Python 3.10+` requirement in the consolidated entrypoint
- install index links to Codex, OpenCode, Claude Code, OpenClaw, migration,
  and uninstall docs

## Outcome

Phase `89` is complete.
`v1.52` now has a consolidated install entry surface.
