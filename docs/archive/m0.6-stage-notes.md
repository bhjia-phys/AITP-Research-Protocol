# M0.6 stage notes

Status: **closed/done under the approved narrowed reviewed claim**. This file
records M0.6 evidence and the post-review M1a transition without changing the
frozen suite inputs. The approved 2026-08-10 [gate-review decision](m0.6-gate-review.md)
was followed by M1a completion at evidence timestamp
`2026-08-10T16:24:52Z`: **done; deterministic gate passed**. It does not claim
bootstrap validation, behavioral or treatment superiority, causal effect, or
AITP advantage over plain files. Detailed M1a evidence is in
[`docs/m1a-stage-notes.md`](m1a-stage-notes.md).

## Implemented gate items

- Item 1: `aitp init --adopt` is implemented and covered by deterministic local
  tests. Recovery evidence records byte-identical before/after evidence for the
  heavy legacy tree; GW_librpa adoption succeeded but its original session
  preserved no before/after tree-hash record. Exact repository/Git-history
  locators for those real-tree records are unavailable, so no claim of two
  preserved real-tree hash records is made.
- Item 2 deterministic runtime portion: `aitp inventory` is implemented with
  deterministic traversal, ordering, and content hashing in a timestamped local
  manifest. Bootstrap Notes, human decision Entries, recall, false-import rate,
  human-time data, and gold-set measurements are **not measured; deferred; not
  counted**.
- Item 3 deterministic suite core: FROZEN v6 suite inputs, seeds, scenarios,
  adapters, rubric, and thresholds are implemented and archived as an
  anchored, unexecuted preregistration. Commit
  `145261805d5205d2150dca18c6c42d5a18a628f2` anchors the frozen bytes; this is
  not scored evidence and cannot retroactively satisfy M0.6.

## 2026-08-09 no-turn scored-run preflight

An operator-only preflight was archived at
`suite/runs/2026-08-09-preflight/S3/` (gitignored run material). No S3, S1, or
S2 participant turn was sent; no gold answer was read for participant use; no
score was produced.

Verified before stopping:

- all 15 FROZEN v6 input hashes matched the freeze record;
- `git status --porcelain suite/` was clean and the v6 anchor was an ancestor
  of the captured HEAD;
- the S3 seed manifest hash matched
  `697326340cd3a5042241c3cde1308fd4a2ed294d33316606a2b26e7ae6890530`;
- runtime remained 1,082 nonblank Python lines; the exact baseline test command
  and output were archived (`26 passed in 0.99s`);
- a treatment delivery bundle was exported from commit `b3396e29`, invoked with
  Python 3.12 `-B`, and verified before/after `aitp --help` to contain the same
  22 runtime files and zero `.pyc` files; its runtime manifest sha256 is
  `4b120cd08b0dce16f73c0ec3eda67f44a156fb4d87b740ef4176e68439243d50`;
- packet neutralization was rehearsed on explicitly unscored dry-run material,
  without gold or scoring.

The frozen protocol prevented a valid S3 start because this execution surface
does not expose exact model/provider plus system/developer-prompt bytes and
agent configuration, and no genuinely separate treatment/control machines or
accounts were provisioned. Raw per-turn instrumentation, sealed condition and
packet-order mappings, independent assessor assignment, and session-2 budgets
were also not established. A same-account namespace was not treated as an
undeclared substitute for the required isolation.

Starting S3 under those conditions would consume the held-out scenario without
valid gate evidence. The operator therefore stopped before the execution-order
draw and first prompt. The preflight is auditable preparation and an explicit
invalid-start decision, **not** a held-out S3 report or paired scored run.

## External harness handoff disposition

The requirements extracted from the preflight are handed off to a separate,
optional future project in the [external evaluation-harness contract](external-evaluation-harness.md).
This is a documentation-only handoff: no harness implementation, dependency,
or runtime change is added to AITP, and FROZEN v6 remains an anchored,
unexecuted preregistration. External output cannot retroactively satisfy the
original M0.6 evidence gaps; any future use as evidence requires separate
review, protocol revision, refreeze, and documentation updates. The invalid-
start decision above is preserved as preparation evidence only.

## Current gate state

The approved narrowed closure is applied:

1. deterministic adoption and inventory implementation/local evidence are
   recorded;
2. bootstrap Notes/decisions, recall, false-import rate, and human time are
   **not measured; deferred; not counted**;
3. held-out S3, paired S1/S2 scores, cold-start, conformance, causal, and
   treatment-advantage evidence are **not measured; deferred; not counted**;
4. M1a is **done; deterministic gate passed**, with detailed evidence in
   [`docs/m1a-stage-notes.md`](m1a-stage-notes.md).

The current CLI is `init`, `enter`, `inventory`, `record`, `note`, `list`,
`show`, and `check`. The two-session ordinary natural-use pause is
complete and the 2026-08-12 reviewed freeze revision
([`docs/m1b-adjudication.md`](m1b-adjudication.md)) selected the read-side
slice M1b-R1, implemented per `docs/m1b-r1-spec.md`; its deterministic gate
passed (evidence recorded in `docs/m1b-r1-stage-notes.md`).
