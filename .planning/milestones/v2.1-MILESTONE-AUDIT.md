# v2.1 Milestone Audit

Status: passed

## Scope Closed

- Phase `175`: Staging Provenance And Noise Suppression
- Phase `175.1`: Topic-Local Consultation Relevance Ordering
- Phase `175.2`: Multi-Paper Real-Topic L2 Relevance Proof

## Requirements Coverage

Score: `5/5` requirements satisfied

- `L2H-01` satisfied by Phase `175` through suppression of generic notation
  bindings and weak `unspecified_method` staging rows before they become
  reusable `L2` artifacts.
- `L2H-02` satisfied by Phase `175` through per-entry preservation of true
  `source_id`, `source_slug`, and related provenance fields across multi-paper
  literature staging.
- `L2H-03` satisfied by Phase `175.1` through bounded consultation-ranking
  changes that allow explicit fresh-topic staged rows to enter
  `primary_hits` ahead of unrelated canonical carryover.
- `L2H-04` satisfied by Phase `175.2` through one isolated multi-paper
  acceptance lane that stages two distinct papers into one fresh topic and
  proves both provenance correctness and relevance ordering.
- `L2H-05` satisfied by Phase `175.2` through durable runbook, summary,
  replay receipts, and explicit non-claims about what this bounded hardening
  slice still does not solve.

## Verification Evidence

- Phase `175` staging hardening:
  - `pytest-literature-intake-support.txt`: `11 passed in 0.90s`
  - `pytest-l2-graph-and-staging.txt`: `10 passed in 0.90s`
- Phase `175.1` consultation ordering:
  - `pytest-l2-graph-activation.txt`: `9 passed in 0.88s`
  - `pytest-aitp-service-consult-l2.txt`:
    `2 passed, 151 deselected in 0.37s`
- Phase `175.2` bounded replay:
  - `pytest-runtime-script.txt`: `1 passed, 80 deselected in 0.36s`
  - `run-multi-paper-l2-relevance.json`: `status = success`
  - `pytest-v2.1-regression-slice.txt`:
    `3 passed, 172 deselected in 0.43s`

## Cross-Phase Integration

- Phase `175` removed the concrete staging defects that made fresh-topic
  multi-paper literature intake untrustworthy: noisy weak rows and collapsed
  source provenance.
- Phase `175.1` then made those cleaned local staged rows actually matter on
  the primary consultation surface instead of trapping them inside
  `staged_hits`.
- Phase `175.2` closed the loop with one replayable fresh-topic proof package
  showing that the earlier fixes compose correctly on a realistic multi-paper
  intake lane.

## Audit Notes

- This milestone follows the same audit convention as `v1.97` through `v2.0`:
  it is audited from phase `SUMMARY.md` files plus fresh replay receipts rather
  than from separately generated per-phase `VERIFICATION.md` files.
- No unsatisfied requirement remains in the `v2.1` traceability table.
- The milestone stayed honest: it hardens fresh-topic staging quality and
  consultation ordering only, not authoritative positive-L2 scientific closure.

## Accepted Open Gaps

- The new measurement-induced / observer-algebra topic is still not promoted as
  authoritative canonical `L2`; this milestone proves bounded staging and
  consultation quality only.
- Global consultation semantics are still only partially hardened; the bounded
  proof covers the explicit fresh-topic staging case rather than a full
  retrieval redesign.
- Front-door fresh-topic `session-start` misrouting and Windows first-use
  source-registration path/status issues remain open and are the next likely
  blockers for broader real-topic natural-language replay.
