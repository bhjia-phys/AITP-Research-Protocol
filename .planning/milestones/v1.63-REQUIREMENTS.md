# Requirements: v1.63 Source Citation BibTeX Surface

## Milestone Goal

Close backlog `999.26` by adding bounded real BibTeX import/export production
paths for Layer 0 sources.

## Active Requirements

### Export

- [x] `REQ-BIB-01`: export one canonical source to a durable `.bib` artifact.
- [x] `REQ-BIB-02`: optionally include the bounded incoming and outgoing
  citation neighborhood in that export.

### Import

- [x] `REQ-BIB-03`: import a bounded BibTeX file into
  `source-layer/topics/<topic_slug>/source_index.jsonl`.
- [x] `REQ-BIB-04`: materialize a durable import report that records imported,
  skipped, and duplicate rows.

### Documentation And Acceptance

- [x] `REQ-BIB-05`: README, kernel docs, source-layer docs, and the source
  catalog acceptance lane document and cover the BibTeX surfaces.

### Verification

- [x] `REQ-VERIFY-01`: close with targeted regressions, maintainability-budget
  verification, and a green full knowledge-hub suite.
