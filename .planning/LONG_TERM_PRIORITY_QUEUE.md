# Long-Term Priority Memory

Status: active priority queue
Updated: 2026-04-15

This file is the durable long-term memory for the current "make AITP truly
usable for research" execution order.

## Current Mainline

### 1. `v2.13` `L2 Content Quality Baseline`

Current status:
- bounded execution complete on the Jones direction
- corpus-growth, usefulness-evaluation, and honest-report artifacts now exist

This milestone proved one bounded real-direction `L2` corpus can become locally
useful. It does not prove broad global `L2` maturity.

### 2. `999.94` Forced `L4` Execution After `L3` Planning

This is now the next live mainline item.

Why:
- the current loop can already sharpen an `L3` route honestly
- the next missing closure is forcing that route into a real `L4` package
  instead of stopping at deferred validation prose

### 3. `999.95` Single-Topic Truth Root And Cross-Adapter Interaction Completion

Core subset only:
- converge runtime/status/current-topic/interaction surfaces on
  `topics/<slug>/...`
- keep legacy roots as compatibility projections only

### 4. Stronger Real-Topic End-to-End Proof

After `999.94` and the core `999.95` path fixes, run a stronger real-topic E2E
proof on the repaired loop.

### 5. Deeper `999.29` Analytical Validation

Pull this forward only if the next active research lane proves it necessary.

## Parallel-Only Work

- `999.93` declarative autonomy profiles
- `999.87` through `999.92` benchmark-aligned extraction and validation fields
- deeper slices of `999.21`, `999.27`, and `999.28`

## Deferred Until Mainline Stabilizes

- `999.44` cross-runtime deep execution parity
- `999.45` multi-user feedback collection
- `999.48` through `999.51` package/install/adoption polish
- `999.1` publication factory
- `999.60` through `999.70` broad CLI and UX polish
- `999.79` through `999.85` graph and DeepXiv expansion
- `999.6` and `999.7` engineering cleanup

## One-Line Memory

Mainline first:
`999.94 -> 999.95(core) -> stronger real-topic E2E -> optional 999.29`.
