# AITP Runtime And Knowledge Foundations

## Current Position

The previously scoped GSD mainline is implemented through:

- `v1.28`
- `v1.29`
- `v1.30`
- `v1.31`
- first `v1.32` slice
- first `v1.33` slice
- `v1.34`
- `v1.35`

That closed the earlier bounded chain, but it did **not** close the broader
AITP collaborator architecture.

## Latest Closed Milestone

The latest scoped milestone was implemented through:

- Phase `38` task-type runtime seeding
- Phase `39` task-type-aware interaction bias
- Phase `40` task-type-by-lane guidance
- Phase `41` collaborator routing and human override guidance

This milestone exists because the main remaining blocker is now clear from a
theoretical-physicist user perspective:

- AITP could expose more protocol state and route posture than before,
- but it still needed to distinguish exploratory work, conjecture-sharpening,
  and target-driven execution as different research jobs,
- and it needed interaction/routing behavior that respects that distinction.

## Latest Milestone Goal

Make AITP organize real theoretical-physics work more like a collaborator by:

1. surfacing `task_type` explicitly as an operational runtime axis,
2. distinguishing `open_exploration`, `conjecture_attempt`, and
   `target_driven_execution` in durable runtime and topic artifacts,
3. making `task_type` shape interaction style and route selection more
   explicitly,
4. and preparing reusable `task_type × lane` orchestration templates for
   actual research work rather than only protocol documentation.

## Latest Closure Evidence

- `.planning/V1.35_CLOSURE_AUDIT.md`

## Active Milestone

Current active milestone:

- `v1.36` `L0 L1 Source Intelligence And Physicist-Grade Intake`

This milestone exists because AITP is still weak at the front of the research
loop from a theoretical-physicist user perspective:

- `L0` still treats source identity and citation proximity too locally,
- `L1` intake is still too shallow on assumptions, regime, notation, and
  contradiction tension,
- and later runtime/layer surfaces risk outrunning the actual quality of
  reading and source intelligence underneath them.

## Active Milestone Goal

Make AITP materially stronger at the front-side research chain by:

1. giving sources more durable identity across related topics,
2. exposing stronger citation-graph and source-neighbor signals,
3. deepening `L1` intake around assumptions, regime, reading depth, and
   contradiction-aware reading,
4. and surfacing the stronger `L0/L1` outputs in runtime/topic read paths so
   later layers can actually use them.

## Active Roadmap

- Phase `42` source identity and citation graph
- Phase `43` assumption and regime intake
- Phase `44` contradiction and notation alignment
- Phase `45` runtime source-intelligence read path

## Important Honesty Boundary

This does **not** mean:

- that AITP now has full `L0-L4` maturity,
- that `L2` is already a rich compounding research brain,
- that source intelligence and physicist-grade reading are already solved,
- or a finished AITP.

It means the bounded `v1.35` chain is closed and `v1.36` is the next
disciplined step.

## Closure History

- `.planning/V1.30_CLOSURE_AUDIT.md`
- `.planning/V1.31_CLOSURE_AUDIT.md`
- `.planning/V1.32_SLICE1_AUDIT.md`
- `.planning/V1.33_SLICE1_AUDIT.md`
- `.planning/V1.34_CLOSURE_AUDIT.md`
- `.planning/V1.35_CLOSURE_AUDIT.md`

## Latest Integrated Regression Evidence

- `138 passed` on 2026-04-09

## Immediate Reality Check

This does **not** mean AITP is finished.

It means the currently scoped `v1.35` chain has been implemented and closed,
and `v1.36` is now the active milestone.

---
*Last updated: 2026-04-09 after opening `v1.36`*
