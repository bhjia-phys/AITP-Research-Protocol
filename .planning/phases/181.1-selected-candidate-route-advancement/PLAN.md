# Phase 181.1 Plan: Selected Candidate Route Advancement

## Objective

Once a durable consultation-followup selection exists, advance the bounded route
onto that selected staged candidate.

## Plan

1. Make queue materialization prefer a selected consultation candidate over the
   generic consultation-followup summary.
2. Expose the new selection artifact through runtime-state pointers and
   must-read surfaces.
3. Cover the route shift with focused queue-materialization regressions.
