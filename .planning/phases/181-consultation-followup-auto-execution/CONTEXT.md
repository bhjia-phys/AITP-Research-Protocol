# Phase 181 Context: Consultation-Followup Auto Execution

## Why this phase exists

After `v2.6`, the route could already surface `consultation_followup`, but the
loop still treated it as a visible-but-non-executable step.

## Bounded goal

Make the post-review consultation step executable and durable without yet
pretending that deeper candidate execution is automatic.
