# Milestone Context

Current milestone: `v1.36` `L0 L1 Source Intelligence And Physicist-Grade Intake`

## Why This Milestone Is Next

From a theoretical-physicist collaborator perspective, the strongest current
mismatch is:

- AITP is now better at classifying task type and steering routes,
- but it still does not read sources or technical literature deeply enough to
  support later layers like a real collaborator,
- and the current runtime risks exposing better orchestration than the source
  and intake substrate underneath can justify.

That makes stronger `L0/L1` source intelligence the right next cycle, not a
premature jump to more downstream orchestration polish.

## What This Milestone Must Protect

- Do not turn `L0` improvement into metadata theater that later layers still
  cannot use.
- Do not treat source presence or citation links as if they were already
  technical understanding.
- Do not let runtime read paths outrun the real quality of source identity and
  intake beneath them.
- Keep `Phase 42` focused on bounded `L0` strengthening before pushing deeper
  `L1` logic into the same slice.

## Ordering Judgment

The milestone was intentionally ordered as:

1. strengthen source identity and citation/source-neighbor signals in `L0`,
2. deepen `L1` assumption, regime, and reading-depth intake,
3. add contradiction and notation-alignment tension signals,
4. then expose the stronger `L0/L1` outputs through runtime/topic read paths.

## Current Status

`v1.35` is closed.

`v1.36` is open.

Current active sequence:

- Phase `42` has context and one implementation plan
- Phases `43` to `45` are declared in the roadmap but not yet planned

Immediate next repository task:

- execute Phase `42` and verify that the stronger `L0` signals materially help
  later reading and routing rather than only renaming fields
